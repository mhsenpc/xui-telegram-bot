name: Java CI with Maven

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:latest
        ports:
          - 3306:3306
        env:
          MYSQL_ROOT_PASSWORD: password
          MYSQL_DATABASE: testdb
          MYSQL_USER: testuser
          MYSQL_PASSWORD: testpassword
        options: >-
          --health-cmd "mysqladmin ping -h localhost"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_HOST: localhost
      DATABASE_PORT: 3306
      DATABASE_NAME: testdb
      DATABASE_USERNAME: testuser
      DATABASE_PASSWORD: testpassword

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Build with Maven
        run: mvn -B package --file pom.xml

      - name: Run tests with Maven
        run: mvn test

      - name: Get the current timestamp
        id: get-timestamp
        run: echo "VERSION=$(date +%Y%m%d%H%M%S)" >> $GITHUB_ENV

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

      - name: Build Docker image
        run: |
          echo "Building docker image"
          docker build -t ${{ secrets.DOCKER_HUB_USERNAME }}/xui_bot:${{ env.VERSION }} -t ${{ secrets.DOCKER_HUB_USERNAME }}/xui_bot:latest

      - name: Push Docker image
        run: |
          echo "Pushing docker image"
          docker push ${{ secrets.DOCKER_HUB_USERNAME }}/xui_bot:${{ env.VERSION }}
          docker push ${{ secrets.DOCKER_HUB_USERNAME }}/xui_bot:latest

      - name: Done
        run: echo "Done"